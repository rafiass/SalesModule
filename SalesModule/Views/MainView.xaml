<UserControl x:Class="SalesModule.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SalesModule"
             xmlns:vm="clr-namespace:SalesModule.ViewModels"
             xmlns:cntrls="clr-namespace:SalesModule.Controls"
             FlowDirection="RightToLeft"
             mc:Ignorable="d" Height="229" Width="378">
    <UserControl.DataContext>
        <vm:MainViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/ResourceDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Command" Value="{Binding OpenCommand}" />
                <Setter Property="Margin" Value="3" />
                <Setter Property="FontSize" Value="15" />
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <TextBlock Text="{TemplateBinding Content}" TextWrapping="Wrap" TextAlignment="Center" />
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="88*" />
            <RowDefinition Height="88*" />
            <RowDefinition Height="50*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Grid.Row="0" Content="קנה וקבל" CommandParameter="{x:Static local:SaleTypes.SingularBuyAndGet}" />
        <Button Grid.Column="1" Grid.Row="0" Content="מבצע (בקרוב)" />
        <Button Grid.Column="2" Grid.Row="0" Content="מוצר מוזל" CommandParameter="{x:Static local:SaleTypes.SingularLowerPrice}" />

        <Button Grid.Column="0" Grid.Row="1" Content="טסט" Command="{Binding TestCommand}" />
        <Button Grid.Column="1" Grid.Row="1" Content="חבילה" />
        <Button Grid.Column="2" Grid.Row="1" Content="קנה וקבל מתקדם (בקרוב)" />

        <Button Grid.ColumnSpan="3" Grid.Row="2" Command="{Binding ManagementCommand}" Content="ניהול מבצעים" Margin="5" />
    </Grid>
</UserControl>
